#pragma once

//Window
const float WIN_WIDTH = 1280.0f;
const float WIN_HEIGHT = 720.0f;
const float WH_RATIO = WIN_WIDTH / WIN_HEIGHT;
const float WINDOW_COORD_MIN_X = -0.5f * WH_RATIO;
const float WINDOW_COORD_MAX_X = +0.5f * WH_RATIO;
const float WINDOW_COORD_MIN_Y = -0.5f;
const float WINDOW_COORD_MAX_Y = +0.5f;